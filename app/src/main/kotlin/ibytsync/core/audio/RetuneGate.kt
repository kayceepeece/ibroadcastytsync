package ibytsync.core.audio

/**
 * Retune gate cut for v1 alongside [RetuneCut]. Kept as a stub so any stale
 * reference fails loudly at compile time instead of silently disabling.
 */
object RetuneGate {
    const val NOTE = "Retune cut for v1: source pitch only. See ticket 09."
}
